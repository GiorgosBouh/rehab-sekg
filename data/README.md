# Data folder

Only `data/samples/` should be included in the public GitHub repository.

The raw `data/external/` folder is excluded from version control. It may
contain downloaded open-dataset files, but those files are not required
for the static demo or the published proof-of-concept.

Raw local clinical documents must not be placed in this folder or
committed to the public repository.
